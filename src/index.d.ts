import { createSelector} from 'reselect';

export const createSelector;

export function setDefaultSelector(newCreateSelector: createSelector);

export function beginScope(scope: any[]);

export function endScope():any[];